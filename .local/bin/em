#! /usr/bin/bash

# echo "checking whether an emacs server is running..."
# emacsclient -a false -e 't'
# return_code=$?
# echo "Return code: $return_code"

# if [[ return_code -ne 0 ]]; then
#     echo "Couldn't find a running Emacs server. Creating a new one..."
#     emacs --daemon
#     echo "Created new emacs server"
#     emacsclient -c -e "(set-theme)" &
# fi

# echo "Starting emacsclient..."
# if  [[ -z "$1" ]]; then
#     echo "no file specified"
# else
#     echo "opening $@"
# fi

# emacsclient --reuse-frame "$@" &
# echo $?

emacsclient --reuse-frame "$@" &
